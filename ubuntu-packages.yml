# packages to add to a bare Ubuntu 20.04 system
#

actions:
  - action: package.install
    list:
      - git
      - jq
      - jo
      - csvkit
      - htop
      - glances
      - mosh
      - curl
      - wget
      - nmap
      - finger
      - pandoc
      - vim
      - bats
      - software-properties-common
      - hub
