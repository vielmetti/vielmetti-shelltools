# packages to add to a bare Ubuntu 20.04 system
#

actions:
  - action: package.install
    list:
      - git
      - jq
      - htop
      - glances
      - mosh
      - curl
      - wget
      - nmap
      - finger
      - pandoc
      - vim
      - software-properties-common
