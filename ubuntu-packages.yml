# packages to add to a bare Ubuntu 20.04 system
#

actions:
  - action: package.install
    name: ubuntu-packages
    list:
      - git
      - jq
      - htop
      - glances
      - mosh
      - curl
      - wget

