# placeholder for loading up ~/bin with trivial but useful shell scripts, stored here

actions:
  - name: setup-tailscale-ubuntu-focal
    action: file.copy
    from: bin/setup-tailscale-ubuntu-focal
    to: /root/.bin/setup-tailscale-ubuntu-focal
    chmod: '700'
